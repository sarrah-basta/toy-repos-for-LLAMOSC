We need to support parsing query parameters for our routes.
Please modify `server/server.py` to extract query parameters from the URL path (e.g., `?key=value`) and pass them as a dictionary to the handler.
